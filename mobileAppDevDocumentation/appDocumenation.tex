\documentclass{article}
\usepackage{listings}

\usepackage{xcolor}
\usepackage{siunitx}

\usepackage{graphicx} %Package to import graphics
\usepackage{fancyhdr}
\usepackage[margin=1in, headheight=30pt]{geometry}

\usepackage[utf8]{inputenc}
\usepackage{booktabs} % For professional table lines

\usepackage{wasysym} % For emojis

\usepackage{url}

\usepackage{minted}

\usepackage[edges]{forest}

\usepackage{tcolorbox}


\definecolor{lightgray}{rgb}{0.95, 0.95, 0.95}

\setminted[kotlin]{
    bgcolor=lightgray,
    linenos=true,   % Optional: adds line numbers
    frame=lines     % Optional: adds a neat top/bottom border
}


\pagestyle{fancy}
\fancyhf{}

% L for Left: logo
\fancyhead[L]{\includegraphics[height=40pt]{images/tujlogo.png}}

\fancyhead[R]{CIS 3515 Introduction to Mobile Application Development}

\fancyfoot[C]{\thepage}

\renewcommand{\footrulewidth}{0.4pt}
\renewcommand{\headrulewidth}{0.3pt}

\begin{document}

    \vspace*{1cm}
    \begin{center}
        {\huge \textbf{CrumbTrails - A Route Tracking Application}} \\
        \vspace{1.5cm}
        {\large Group Members:} \\
        \vspace{0.25cm}
        {\large  Adam James Roy, Ayra Baig, Ibrahim Sajid} \\
        \vspace{1.5cm}
        {\small July 10, 2026}
    \end{center}

    \vspace{1cm}
    \begin{abstract}
        CrumbTrails is an Android application that aims to solve one problem for runners, drivers, motorcyclists,
        and many more: tracking your aimless routes. Whether it is a joyride in a car or trying to find a more 
        interesting running route, CrumbTrails will remember the trail that you have traveled, allowing users to
        look back on their route and place photos along that path automatically. Once having saved a route, these
        are also able to be shared amongst other users in the CrumbTrail community, finding more beautiful scenery,
        tasty pizza joints... Only your imagination is the limit!
    \end{abstract}


    \newpage

    \tableofcontents

    \newpage

    \section{Project Overview}

        \subsection{Main Features and Functionality}

            Crumb Trails is centered around three main features:

            \begin{enumerate}
                \item Tracking the route taken by the user
                \item Saving the routes in a local database
                \item Sharing and viewing routes of other users
            \end{enumerate}

            Tracking the route of the user is performed via the onboard GPS of the 
            smartphone and toggled via a simple button on the main map screen. Once 
            activated, the user can either keep the screen live, or sleep the device. 
            If the latter is chosen, the tracking functionality is moved to a foreground
            service, providing a notification that the tracking is working in the background.
            Upon returning to the app, the user can press the toggle again to halt tracking, 
            triggering a dialog box to enter the name of the route, completing the 
            tracking process.
            
            Once the route is added, it is then saved into the local database, also configured
            in this application. Moving to the history tab, these routes are viewable, each taking
            the form of a card view, allowing the users to scroll through their past trips. Upon clicking
            a card, a dialog box is shown to preview the line of the route in Google Maps. Each 
            card also has buttons integrating functions to add photos to the route which 
            appear on the map, as well as delete and share. Sharing then sends the route to 
            a cloud-based database linked to the user's'account created upon startup of the 
            application.

            Sharing sends the route, along with the choosen photos to the server. This is then shown
            in the feed section of the application. Posts from other uses are also displayed here,
            where everyone can discover new routes that they have not known existed!

        \subsection{Motivation for Creation}

            Motivated from personal woes, Crumb Trails was created to solve the one problem that comes
            with any joy rides, aimless walks, or curious bicycle trips. When finishing a trip, 
            "where was that beautiful piece of tarmac again?" or "man, I would love to make that my 
            run route for now on... But what turn did I take to get there?" - These thoughts may 
            come to mind wishing you wrote it on a map as when went. Crumb Trails main feature is
            centered around solving this problem, giving users ways to share and record these routes
            for ease of use.

            This feature, as simple as it sounds, does not exist in a form that is user-friendly
            \textbf{AND} reliable. For example, Calimoto is an existing cross-platfrom application
            that is feature-rich and clean interface. However, this comes at a cost to use their 
            route tracking feature, hiding it behind a subscription paywall. Furthermore, Google Maps
            has a route tracking feature but is plagued with reliability issues and does not give the 
            user enough control to know how the app is tracking their route. These issues are aimed to 
            be resolved and its architecture and design to achieve this is explained below.

            \textbf{NOTE}: While not free, this application is far less expensive when using your 
            self-supplied API keys. Further development is needed for viable deploy plans.

    \newpage

    \section{Technical Architecture}

        This Android application, built in Android Studio, utilizes a one activity architecture,
        local database where applicable, as well as cloud database base to focus on giving the user
        a lightning fast UI experience. Choices for external and internal APIs reflect these
        design decisions also allowing for scalability and maintainability in mind. These choices
        consist of Room, a local database option which offers an abstraction layer on top of SQlite3.
        Google's Firebase that has the role of managing authentication, as well as creating the 
        environment for users to build a route-sharing community. The internal API was designed 
        around a MVVM (model-view-view model) architecture to maintain a highly readable, scalable 
        structure.

        \subsection{Frontend}

        Each feature that has been implemented that is directed interfaced with by the user
        is found in the \textbf{featuresAPI} directory. Each directory name corresponds with
        the name of the feature/function and consists of their MVVM components. This gives 
        flexibilty to decouple the Jetpack Compose functions from the logic, as well as 
        functions that will directly interface with the Room database. In the case of complex
        views such as the \textbf{history/ui}, this may be further decoupled to create
        smaller UI components.

        \subsection{Backend}

        Managing data locally was executed by creating a Room database, which mentioned above,
        uses SQLite3 as its functional base. Made of three distinct parts, Room is utilized to
        perform typical CRUD operation on the table containing the user's tracked routes,
        the time that the route was taken as well as directory paths of photos that were added
        post-trip. This project has one table for the routes, therefore, three main componenets;
        the \textbf{TrackedRouteEntity}, \textbf{TrackedRouteDao}, and \textbf{TrackedRouteRepository}. 
        These are then connected via the \textbf{RouteTrackerDatabase}, which is then instantiated 
        in the history tab. By using the functions supplied in the DAO, or data access object, the 
        frontend as a fully decoupled layer that can access the database through the SQlite3 queries.

    \newpage

            \subsection*{\hfil Important Data Classes \hfil}

            When saving each route to the local Room database,
            the data class below, \textbf{TrackedRoute}, was created:

            \begin{center}
                \begin{minted}{kotlin}
data class TrackedRoute(
    val id: String,
    val tripName: String,
    val startedAtEpochMillis: Long,
    val endedAtEpochMillis: Long,
    val trackedRoute: List<LatLng>
)
                \end{minted}
            \end{center}

            The tracked data class has a couple unique traits in the form of its parameters. First, 
            when the time stamp for beginning the trip and ending is measured by \textbf{Epoch time}.
            This is the time elapsed in milliseconds from January 1st, 1970. These two long values 
            will be used to determine the time of the trip, and how long the trip lasted for. Secondly, 
            the trackedRoute parameter is a list of tuples representing the gathered points latitude and
            longitude values (decimal form). Entering this into the database then requires encoding
            of the trackedRoute LatLng list, into a string using Google Maps PolyUtil API. *See Backend 
            Implementation

            \begin{center}
                \begin{minted}{kotlin}
data class SharedRoutePost(
    val postId: String = "",
    val userId: String = "",
    val authorName: String = "",
    val authorUsername: String = "",
    val routeId: String = "",
    val tripName: String = "",
    val caption: String = "",
    val routeString: String = "",
    val createdAt: Long = 0L,
    val pointCount: Int = 0,
    val photoUrls: List<String> = emptyList(),
    val photos: List<SharedRoutePhoto> = emptyList()
)
                \end{minted}
            \end{center}


    \newpage

    \section{API Design}



        \noindent
        % The Tree Box 
        \begin{minipage}[t]{0.55\textwidth}
            \begin{forest}
                for tree={%
                    folder,
                    grow'=0,
                    fit=band,
                }
                [java
                    [com.example.routetracker
                        [ui.theme
                            [Color.kt]
                            [Theme.kt]
                            [Type.kt]
                        ]
                        [utils
                            [Converters]
                            [PhotoDownsizer]
                        ]
                        [MainActivity.kt]
                    ]
                    [data
                        [local
                            [track
                                [TrackedRoute]
                                [TrackedRouteDao]
                                [TrackedRouteEntity]
                                [TrackedRouteRepository]
                            ]
                            [RouteTrackerDatabase]
                        ]
                        [location
                            [MapRepository]
                        ]
                    ]
                    [featuresAPI
                        [authentication]
                        [feed]
                        [history]
                        [navigation]
                        [settings]
                        [shared]
                        [tracking]
                    ]
                ]
                [res
                    [Drawables contained here]
                ]
            \end{forest}
        \end{minipage}
        \hfill 
        \begin{minipage}[t]{0.50\textwidth}
            \begin{tcolorbox}[title=Architecture Notes, colback=white, colframe=purple]
                \textbf{Directory Overview:} \\
                This package structure separates core app logic from the modular feature APIs.
                
                \vspace{0.5em}
                \textbf{Key Components:}
                \begin{itemize}
                    \item \texttt{data/}: Handles local Room databases and map (GPS) logic.
                    \item \texttt{featuresAPI/}: Contains isolated feature modules to support scalability. Each directory follows MVVM format.
                \end{itemize}
                \textbf{NOTE}: Other files not included here have been modified such as gradle build files. See 
                Section 4 Requirements and Installation Instructions for more details.
            \end{tcolorbox}
        \end{minipage}

        \subsection{Backend Implementaion} The following are functions that are substantial to
        for understanding the design choices taken for this project.

            \subsection*{Function: \texttt{insert(route: TrackedRouteEntity)}}

                Uses the Room annotation \textbf{@Insert} to indicate that will be inserted into
                the database.\\
                Source: data/local/track/TrackedRouteDao, line 13
                
                \subsection*{Parameters}

                \begin{tabular}{p{3cm} p{4cm} p{7cm}}
                \toprule
                \textbf{Name} & \textbf{Type} & \textbf{Description} \\ \midrule
                \texttt{route} & TrackedRouteEntity & An instance of the corresponding entity 
                \end{tabular}

                \paragraph{Returns} \texttt{Unit} - The equivalent of void

            \hrulefill

            \subsection*{Function: \texttt{getAll() : Flow<List<TrackedRouteEntity>>}}

                Uses the Room annotation \textbf{@Query} with the accompanying argument to retrieve the database elements
                in descending order. \\
                Source: data/local/track/TrackedRouteDao, line 17
                
                \subsection*{Parameters}

                \begin{tabular}{p{3cm} p{4cm} p{7cm}}
                \toprule
                \textbf{Name} & \textbf{Type} & \textbf{Description} \\ \midrule
                \texttt{No parameters} \\ \bottomrule
                \end{tabular}

                \paragraph{Returns} \texttt{Flow<List<TrackedRouteEntity>>} - A flow list of the database's stored routes.
                Note that this is different from a normal list as this remains active to adjust to any database changes.
                Mainly used for displaying the user's past routes in the History page.

            \hrulefill

            \subsection*{Function: \texttt{getById(id: String): TrackedRouteEntity?}}

                Retrieves the route data from a specific ID. This has been implemented mainly
                for sending the route do the Firebase server to share to the feed page. \\
                Source: data/local/track/TrackedRouteDao, line 21
                
                \subsection*{Parameters}

                \begin{tabular}{p{3cm} p{4cm} p{7cm}}
                \toprule
                \textbf{Name} & \textbf{Type} & \textbf{Description} \\ \midrule
                \texttt{id} & String & ID saved in the route entity \\ \bottomrule
                \end{tabular}

                \paragraph{Returns} \texttt{TrackedRouteEntity?} - The route entity is returned if the 
                id exists in the database, Unit if not.

            \newpage

            \subsection*{Function: \texttt{delete(id: String)}}

                Removes a route entity from the database specified by its ID. \\
                Source: data/local/track/TrackedRouteDao, line 24
                
                \subsection*{Parameters}

                \begin{tabular}{p{3cm} p{4cm} p{7cm}}
                \toprule
                \textbf{Name} & \textbf{Type} & \textbf{Description} \\ \midrule
                \texttt{id} & String & ID saved in the route entity \\ \bottomrule
                \end{tabular}

                \paragraph{Returns} \texttt{Unit} 

            \hrulefill

            \subsection*{Function: \texttt{updatePhotoPaths(id: String, photoPaths: List<String>)}}

                Updates the path of the photos that are associated with the route
                specified. A photo path is handed over by the PhotoDownsizer object.
                The TypeConverter is also used for Room to accept the list. \\
                Source: data/local/track/TrackedRouteDao, line 27
                
                \subsection*{Parameters}

                \begin{tabular}{p{3cm} p{4cm} p{7cm}}
                \toprule
                \textbf{Name} & \textbf{Type} & \textbf{Description} \\ \midrule
                \texttt{id} & String & ID saved in the route entity \\ \addlinespace
                \texttt{photoPaths} & \texttt{List<String>} & Containing the path to each photo in local storage \\ \bottomrule
                \end{tabular}

                \paragraph{Returns} \texttt{Unit} 

            \hrulefill

            \subsection*{Function: \texttt{save(startedAtEpochMillis: Long, endedAtEpochMillis: Long, path: List<LatLng>, tripName: String)}}

                Utlizing the DAO's insert function, the parameters passed through this function
                are then used to create a TrackedRouteEntity once a trip is completed. Executed 
                in a background thread as it is a suspend function. \\
                Source: data/local/track/TrackedRouteRepository, line 18
                
                \subsection*{Parameters}

                \begin{tabular}{p{3cm} p{4cm} p{7cm}}
                \toprule
                \textbf{Name} & \textbf{Type} & \textbf{Description} \\ \midrule
                \texttt{id} & String & ID saved in the route entity \\ \addlinespace
                \texttt{photoPaths} & List<String> & Containing the path to each photo in local storage \\ \bottomrule
                \end{tabular}

                \paragraph{Returns} \texttt{Unit} 

            \hrulefill

            \newpage

            \subsection*{Function: \texttt{addPhotoPaths(id: String, newPaths: List<String>)}}

                In a background thread, a variable is made to save the existing list of
                paths and then concatenate the new threads onto that list. \\
                Source: data/local/track/TrackedRouteRepository, line 43
                
                \subsection*{Parameters}

                \begin{tabular}{p{3cm} p{4cm} p{7cm}}
                \toprule
                \textbf{Name} & \textbf{Type} & \textbf{Description} \\ \midrule
                \texttt{id} & String & ID saved in the route entity \\ \addlinespace
                \texttt{newPhotoPaths} & \texttt{List<String>} & Containing paths that will be concatenated \\ \bottomrule
                \end{tabular}

                \paragraph{Returns} \texttt{Unit} 

            \hrulefill

            \subsection*{Function: \texttt{removePhotoPaths(id: String, pathToRemove: List<String>)}}

                In a background thread, a variable is made to save the existing list of
                paths and passes the existing path with the path to remove through the dao
                function. runCatching is then finally used to remove the corressponding photo from
                memory. \\
                Source: data/local/track/TrackedRouteRepository, line 51
                
                \subsection*{Parameters}

                \begin{tabular}{p{3cm} p{4cm} p{7cm}}
                \toprule
                \textbf{Name} & \textbf{Type} & \textbf{Description} \\ \midrule
                \texttt{id} & String & ID saved in the route entity \\ \addlinespace
                \texttt{pathToRemove} & \texttt{List<String>} & Path that will be subtracted from the current path. \\ \bottomrule
                \end{tabular}

                \paragraph{Returns} \texttt{Unit} 

            \hrulefill

            \newpage
    
            \subsection*{Function: \texttt{loginWithEmail(email: String, wordPass: String): Flow<Resource<String>>}}

                Using the \textbf{FirebaseAuth} object, this function passing the 
                corresponding parameters to login with their email and password.\\
                Source: featuresAPI/authentication/data/AuthenticationRepository, line 16
                
                \subsection*{Parameters}

                \begin{tabular}{p{3cm} p{4cm} p{7cm}}
                \toprule
                \textbf{Name} & \textbf{Type} & \textbf{Description} \\ \midrule
                \texttt{email} & String & ID saved in the route entity \\ \addlinespace
                \texttt{wordPass} & String & Path that will be subtracted from the current path. \\ \bottomrule
                \end{tabular}

                \paragraph{Returns} \texttt{Flow<Resource<String>>} - A live data pipeline that outputs
                the current state of the function process, enabling the UI to update it in real time.
                Depending on the output, it will allow the UI to show a loading symbol, etc.

            \hrulefill

            \subsection*{Function: \texttt{signUpWithEmail(email: String, wordPass: String): Flow<Resource<String>>}}

                Using the \textbf{FirebaseAuth} object, this function passing the 
                corresponding parameters to make a new in the Firebase server
                with their email and password. \\
                Source: featuresAPI/authentication/data/AuthenticationRepository, line 16
                
                \subsection*{Parameters}

                \begin{tabular}{p{3cm} p{4cm} p{7cm}}
                \toprule
                \textbf{Name} & \textbf{Type} & \textbf{Description} \\ \midrule
                \texttt{email} & String & ID saved in the route entity \\ \addlinespace
                \texttt{wordPass} & String & Path that will be subtracted from the current path. \\ \bottomrule
                \end{tabular}

                \paragraph{Returns} \texttt{Flow<Resource<String>>} - A live data pipeline that outputs
                the current state of the function process, similar to the above function.

            \newpage

        \subsection{Frontend Implementation} Similar to the previous Backend Implementation section, 
        below contains notable functions created for this application:

            \subsection*{Function: \texttt{RouteTrackerNavGraph(navController: NavHostController, authRepository: AuthenticationRepsitory, 
            authViewModel: AuthenticationViewModel, modifier: Modifier = Modifier)}}

                The main composable function that allows the user to go between the different screens and 
                functions. In the MainActivity, this is utilized in the form of a bottom navigation bar.
                The different routes are defined in the RouteTrackerDestination enum file. \\
                Source: featuresAPI/navigation/RouteTrackerNavGraph.kt, line 18

                \subsection*{Parameters}

                \begin{tabular}{p{3cm} p{4cm} p{7cm}}
                \toprule
                \textbf{Name} & \textbf{Type} & \textbf{Description} \\ \midrule
                \texttt{navController} & NavHostController & Android's offered controller passed through \textbf{NavHost} \\ \addlinespace
                \texttt{authRepository} & AuthenticationRepository & Needed to check if user is logged in \\ \addlinespace
                \texttt{authViewModel} & AuthenticationViewModel & Need for logging the user out from SettingsUI \\ \addlinespace
                \texttt{modifier} & Modifier & Defaulted as Modifier. For Jetpack Compose \\ \bottomrule
                \end{tabular}

                \paragraph{Returns} \texttt{Unit}

            \hrulefill


            \subsection*{Function: \texttt{startTracking()}}

                This function has the role of both starting the starting the tracking service
                and moving it to the background as a foreground service and building the notification
                stating that the tracking is ongoing. Once initiated, a variable in the class 
                \textbf{trackingJob} is then modified in a background thread, colleting the path 
                points that form the route (LatLng). An Intent is then used in the TrackingViewModel
                to start and to pass the data between the two. \\
                Source: featuresAPI/tracking/service/TrackingService, line 84

                \subsection*{Parameters}

                \begin{tabular}{p{3cm} p{4cm} p{7cm}}
                \toprule
                \textbf{Name} & \textbf{Type} & \textbf{Description} \\ \midrule
                \texttt{No Parameters} &
                \end{tabular}

                \paragraph{Returns} \texttt{Unit}

            \newpage

            \subsection*{Function: \texttt{stopTracking(tripName: String)}}

                Activated when the user presses the toggle for tracking. When pressed,
                an alert dialog appears, prompting the user to enter the desired name for
                the trip, which is entered into this function as a parameter. Once called,
                the \textbf{trackingJob} variable is set to null to signal it is finished. 
                As long as there were points saved from the tracking procedure, a background 
                thread is then created to save the details into the database as a 
                TrackedRouteEntity. Once added, the foreground service is then stopped. \\
                Source: featuresAPI/tracking/service/TrackingService, line 109

                \subsection*{Parameters}

                \begin{tabular}{p{3cm} p{4cm} p{7cm}}
                \toprule
                \textbf{Name} & \textbf{Type} & \textbf{Description} \\ \midrule
                \texttt{tripName} & String & User entered name for the trip
                \end{tabular}

                \paragraph{Returns} \texttt{Unit}

            \hrulefill

            \subsection*{Function: \texttt{shareRouteToFeed(route: TrackedRoute, caption: String)}}

                This function is used when the user shares a saved route from the History
                screen. Each saved session card contains a share button, which opens an alert
                dialog asking the user to add a caption before posting the route. Once the user
                confirms the share action, the selected route/session and caption are sent through
                the HistoryViewModel. Any photos already attached to the route are converted into
                photo URIs and included in the upload. The route points are encoded into a
                \textbf{routeString} so the Feed can redraw the shared route later. If photos are
                included, they are uploaded to Firebase Storage first, and the shared post data is
                then saved for Feed display. The Feed screen listens to shared route posts and
                displays routes from users in the application. \\
                Source: featuresAPI/history/ui/HistoryUI.kt, line 118; featuresAPI/history/viewModel/HistoryViewModel.kt, line 74; featuresAPI/feed/data/FeedRepository.kt, line 34

                \subsection*{Parameters}

                \begin{tabular}{p{3cm} p{4cm} p{7cm}}
                \toprule
                \textbf{Name} & \textbf{Type} & \textbf{Description} \\ \midrule
                \texttt{route} & TrackedRoute & Saved route/session selected from History \\ \addlinespace
                \texttt{caption} & String & User entered caption for the shared Feed post \\ \bottomrule
                \end{tabular}

                \paragraph{Returns} \texttt{Unit}

            \hrulefill

             \subsection*{Function: \texttt{fun()}}

                Nothing yet \\
                Source: featuresAPI/tracking/service/TrackingService, line 109

                \subsection*{Parameters}

                \begin{tabular}{p{3cm} p{4cm} p{7cm}}
                \toprule
                \textbf{Name} & \textbf{Type} & \textbf{Description} \\ \midrule
                \texttt{No parameters} 
                \end{tabular}

                \paragraph{Returns} \texttt{Unit}

            \newpage

            \section{Requirements and Installation Instructions}
                
                \subsection{System Requirements}

                    Below are the requirements to build, run, and develop the project.

                    \subsubsection*{Development Environment}

                        \begin{center}
                            \begin{tabular}{p{5cm} p{7cm}}
                                \toprule
                                \textbf{Requirement} & \textbf{Version / Detail} \\ \midrule
                                    OS & Windows, macOS, or Linux (Android Studio compatible OS) \\ \addlinespace
                                    IDE & Android Studio (recent version, compatible with AGP 9.1.1 / Gradle 9.3.1) \\ \addlinespace
                                    JDK & \textbf{JDK 21} \\ \addlinespace
                                    Android Gradle Plugin (AGP) & 9.1.1 \\ \addlinespace
                                    Kotlin & 2.2.10 \\ \addlinespace
                                    KSP & 2.2.10-2.0.2 \\ \bottomrule
                            \end{tabular}
                        \end{center}

                    \subsubsection*{Android SDK / Device Requirements}

                    \begin{center}
                        \begin{tabular}{p{5cm} p{7cm}}
                            \toprule
                            \textbf{Setting} & \textbf{Value} \\ \midrule
                            \texttt{compileSdk} & 37 \\ \addlinespace
                            \texttt{targetSdk} & 36 \\ \addlinespace
                            \texttt{minSdk} & 34 (Android 14+) \\ \addlinespace
                                Java source/target compatibility & Java 11 \\ \addlinespace
                                Compose & Enabled (\texttt{buildFeatures.compose = true}) \\ \addlinespace
                                BuildConfig & Enabled \\ \bottomrule
                        \end{tabular}
                    \end{center}

                    Since \texttt{minSdk = 34}, the app only installs/runs on devices running \textbf{Android 14 (API 34) or newer}.

                    \subsubsection*{Required Gradle/Android Plugins}

                        \begin{itemize}
                            \item \texttt{com.android.application} --- 9.1.1
                            \item \texttt{org.jetbrains.kotlin.plugin.compose} --- 2.2.10
                            \item \texttt{com.google.devtools.ksp} --- 2.2.10-2.0.2 (annotation processing for Room)
                            \item \texttt{com.google.android.libraries.mapsplatform.secrets-gradle-plugin} --- 2.0.1 (used for taking the Google Maps API key from \texttt{local.properties})
                            \item \texttt{com.google.gms.google-services} --- 4.4.2 (Firebase configuration)
                        \end{itemize}

                    \subsubsection*{External Services / Necessary Accounts}

                    Two files are deliberately excluded from the repo (\texttt{.gitignore}) and \textbf{must be supplied locally} before the project will build/run:

                    \begin{enumerate}
                        \item \textbf{\texttt{local.properties}} (project root) --- must define a Google Maps API key, e.g.:
                        \begin{center}
                            \begin{minted}{text}
            GOOGLE_MAPS_API_KEY=your_key_here
                            \end{minted}
                        \end{center}
                        Consumed by the secrets-gradle-plugin and injected into the manifest placeholder \texttt{\$\{GOOGLE\_MAPS\_API\_KEY\}}.
                        \item \textbf{\texttt{app/google-services.json}} --- Firebase project configuration file, downloaded from the Firebase console. Required for Firebase Auth, Realtime Database, and Storage to function.
                    \end{enumerate}

                    You will therefore need:
                    \begin{itemize}
                        \item A \textbf{Google Cloud / Maps Platform} project with the Maps SDK for Android enabled (billing-enabled API key).
                        \item A \textbf{Firebase} project with Authentication, Realtime Database, and Storage enabled.
                    \end{itemize}

                    \subsubsection*{App Permissions (Pre-declared in AndroidManifest.xml)}

                        \begin{itemize}
                            \item \texttt{ACCESS\_FINE\_LOCATION}
                            \item \texttt{ACCESS\_COARSE\_LOCATION}
                            \item \texttt{INTERNET}
                            \item \texttt{FOREGROUND\_SERVICE}
                            \item \texttt{FOREGROUND\_SERVICE\_LOCATION}
                            \item \texttt{POST\_NOTIFICATIONS}
                        \end{itemize}

                    (Used for GPS route tracking via a foreground \texttt{TrackingService}, and notifying the user while tracking is active.)

                    \subsubsection*{Library Dependencies}

                     \paragraph{AndroidX / Jetpack}

                        \begin{center}
                            \begin{tabular}{p{7cm} p{4cm}}
                            \toprule
                            \textbf{Library} & \textbf{Version} \\ \midrule
                                androidx.core:core-ktx & 1.19.0 \\ \addlinespace
                                androidx.lifecycle:lifecycle-runtime-ktx & 2.11.0 \\ \addlinespace
                                androidx.lifecycle:lifecycle-viewmodel-compose & 2.8.2 \\ \addlinespace
                                androidx.activity:activity-compose & 1.13.0 \\ \addlinespace
                                androidx.compose:compose-bom (BOM) & 2024.09.00 \\ \addlinespace
                                androidx.compose.ui:ui & via BOM \\ \addlinespace
                                androidx.compose.ui:ui-graphics & via BOM \\ \addlinespace
                                androidx.compose.ui:ui-tooling / ui-tooling-preview & via BOM \\ \addlinespace
                                androidx.compose.material3:material3 & via BOM \\ \addlinespace
                                androidx.compose.material:material-icons-extended & via BOM \\ \addlinespace
                                androidx.navigation:navigation-compose & 2.9.8 \\ \addlinespace
                                androidx.room:room-runtime, room-ktx, room-compiler (KSP) & 2.8.4 \\ \addlinespace
                                androidx.exifinterface:exifinterface & 1.4.1 \\ \addlinespace
                                androidx.photopicker:photopicker-compose & 1.0.0-alpha01 \\ \bottomrule
                            \end{tabular}
                        \end{center}

                        \paragraph{Google / Firebase / Maps}

                        \begin{center}
                            \begin{tabular}{p{7cm} p{4cm}}
                            \toprule
                            \textbf{Library} & \textbf{Version} \\ \midrule
                                com.google.android.gms:play-services-location & 21.3.0 \\ \addlinespace
                                com.google.android.gms:play-services-maps & 19.0.0 \\ \addlinespace
                                com.google.maps.android:maps-compose & 6.0.0 \\ \addlinespace
                                com.google.maps.android:android-maps-utils & 3.20.1 \\ \addlinespace
                                com.google.firebase:firebase-bom (BOM) & 33.1.2 \\ \addlinespace
                                com.google.firebase:firebase-auth-ktx & via BOM \\ \addlinespace
                                com.google.firebase:firebase-storage & via BOM \\ \addlinespace
                                com.google.firebase:firebase-database & via BOM \\ \addlinespace
                                firebase-database-ktx (catalog entry) & 22.0.1 \\ \bottomrule
                            \end{tabular}
                        \end{center}

                        \paragraph{Utilities}

                        \begin{center}
                            \begin{tabular}{p{7cm} p{4cm}}
                            \toprule
                            \textbf{Library} & \textbf{Version} \\ \midrule
                                io.coil-kt:coil-compose\ (image loading) & 2.7.0 \\ \addlinespace
                                com.google.code.gson:gson & 2.10.1 \\ \bottomrule
                            \end{tabular}
                        \end{center}

                \subsection{Installation Instructions}

                \begin{enumerate}
                    \item Install \textbf{JDK 21}.
                    \item Install \textbf{Android Studio}.
                    \item Clone the repository.
                    \item Create a \textbf{Google Maps API key} and add it to \texttt{local.properties} as \texttt{GOOGLE\_MAPS\_API\_KEY}.
                    \item Create a \textbf{Firebase project}, download \texttt{google-services.json}, and place it in \texttt{/app}.
                    \item Ensure a target device/emulator runs \textbf{Android 14 (API 34) or newer}.
                    \item Sync Gradle and build --- internet access is required for Gradle/Maven dependency downloads and for the app itself (Maps, Firebase) at runtime.
                \end{enumerate}

            \newpage
                    
                    

            \section{Testing}

                \subsection{Summary}

                The following is our testing method for ensuring a complete, safe
                application environment:

                \begin{enumerate}
                    \item Testing in a real-world scenario to ensure expected results are consistent
                    \item Test for edge cases (Unintended use cases, etc.)
                    \item Clean installation to simulate a new user installing the application.
                \end{enumerate}

                \newpage
                
                \subsection{Known Limitations}

                Currently, the largest known limitation is the issue of a deploying strategy. As stated before, this application uses
                Google's API platforms for both the Maps and Firebase services. Going forward, our plan is to create a way to monetize 
                the application, without causing the user overly to take the burden. A current candidate would be to place small adds
                after saving a route and, in the best poosible case, have adds as relevant and interesting as possible. An example of 
                this could include newly released motorcycle gear, etc.

                Another limitation which is a priority is, due to the time crunch of this project, photos are costly to upload to the
                feed. Now, we have the infrusture to implement a fix. Currently there is a photo downsizer object that is currently 
                used to save the copy of the uploaded photo. Fixing the logic to have the application send that downsized version instead 
                of the original will fix this issue.

                Lastly, there is one bug causing the polyline to not update correctly in real-time. As the application is tracking, the 
                logic is in place to have the blue polyline to give the user the track that they have traveled. It is not reliable at
                the moment, and fixes for this is in the pipeline.

            \newpage

            \section{Attributions and Citations}

                \subsection{Cited Works}

                    As the project virtually entirely used Google API platforms,
                    when the Android Developers Documentation lacked depth, Gemini was
                    resorted to to answer lingering questions. When possible, learning 
                    from the documentation was most crucial to learning the ins and outs
                    of Android Studio, Kotlin, and Jetpack Compose. The following were used 
                    in this development process:

                    \begin{enumerate}
                        \item \textbf{States With Jetpack Compose}: \url{https://developer.android.com/develop/ui/compose/state?hl=ja}
                        \item \textbf{Dialog Composables}: \url{https://developer.android.com/develop/ui/compose/components/dialog?hl=ja}
                        \item \textbf{Type Converters for Room}: \url{https://developer.android.com/training/data-storage/room/referencing-data?hl=ja}
                        \item \textbf{General Room Explanation (DAO, Entities, Repositories)}: \url{https://developer.android.com/training/data-storage/room/defining-data?hl=ja}
                        \item \textbf{Firebase - Application Connection}: \url{https://firebase.google.com/docs/auth/android/start?hl=ja}
                        \item \textbf{Embedded Photo Picker}: \url{https://developer.android.com/training/data-storage/shared/photo-picker/embedded?hl=ja}
                        \item \textbf{NavController and NavHost Setup}: \url{https://developer.android.com/codelabs/basic-android-kotlin-compose-navigation?hl=ja}
                        \item \textbf{StateFlow and LiveData}: \url{https://developer.android.com/kotlin/flow/stateflow-and-sharedflow?hl=ja}
                        \item \textbf{Switching to Foreground Service}: \url{https://developer.android.com/develop/background-work/services/fgs/restrictions-bg-start}
                        \item \textbf{runCatching}: \url{https://proandroiddev.com/kotlin-tips-and-tricks-you-may-not-know-7-goodbye-try-catch-hello-trycatching-7135cb382609}
                    \end{enumerate}

                \subsection{Contributions}

                    \paragraph{\textbf{Adam}} Created the local Room database structure, their accompanying files, 
                    and focused on creating the layout of the API. Implemented the Composables necessary to get the general
                    functionality including for tracking, history, feed, and authentication. Created LaTeX layout used to 
                    generate this documentation.

                    \paragraph{\textbf{Ayra}}

                    \paragraph{\textbf{Ibrahim}} \smiley 

    \end{document}
